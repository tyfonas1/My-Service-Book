// colors
// /////////////////////////////////////////////////////////////////////////////

const primary = '#cf2047'; // - secondary color for your app which complements the primary color.
const accent = '#df1e33'; // - primary color for your app, usually your brand color.
const background = '#fff'; // - background color for pages, such as lists.
const surface = '#f8f8f8'; // - background color for elements containing content, such as cards.
const text = '#525252'; // - text color for content.
const disabled = '#AEC3B0'; // - color for disabled elements.
const placeholder = '#acacac'; // - color for placeholder text, such as input placeholder.
const backdrop = 'rgba(42, 42, 42, 0.3)'; // - color for backdrops of various components such as modals.
const border = '#3C4650';
const error = '#f44336';
const blue = '#3f51b5';
const white = '#fff';
const lighBorder = 'rgba(42, 42, 42, 0.1)';

export default {
  primary,
  accent,
  background,
  surface,
  text,
  disabled,
  placeholder,
  backdrop,
  border,
  error,
  blue,
  white,
  lighBorder,
};
